<!DOCTYPE html>
<html lang="{{ site.language }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    {%~ include 'partials/metatags.html.twig' ~%}
    <style>
      {% apply scss_to_css|minify_css -%}
      {%- block css -%}
      $background: #000;
      $color: #fff;
      $margin: 2px;
      $height: 300px;
      body {
        font: 16px/24px -apple-system, Roboto, Helvetica, Arial, sans-serif;
        background: $background;
        color: $color;
        margin: $margin;
      }
      a {
        color: $color;
      }
      footer {
        margin: 10px;
        text-align: right;
      }
      {%- endblock css -%}
      {%- endapply ~%}
    </style>
  </head>
  <body>
    <main>
      {%- block content ~%}
      {{ page.content }}
      {%- endblock ~%}
    </main>
    <footer>
      {%- if site.author.name|default -%}
        {%- set author -%}
        {{ site.author.name }}
        {%- endset -%}
      {%- endif -%}
      {%- if site.author.name|default and site.author.url|default -%}
        {%- set author -%}
        <a href="{{ url(site.author.url) }}">{{ site.author.name }}</a>
        {%- endset -%}
      {%- endif -%}
      {%- set powered_by -%}
      <a href="https://photo-stream.cecil.app">Photo Stream</a> &amp; <a href="{{ cecil.url }}">Cecil</a>
      {%- endset ~%}
      {% if author|default %}{% trans %}Photos from %author%{% endtrans %} - {% endif %}{% trans %}Powered by %powered_by%{% endtrans ~%}
    </footer>
  </body>
</html>